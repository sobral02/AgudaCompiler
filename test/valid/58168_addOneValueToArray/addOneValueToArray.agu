-- Author: 58168, Henrique Costa Vale

let f(a,b) : (Int [] , Int) -> Bool =
  let value : Bool = false; 
  let c : Int [] = new Int[length(a) + 1 | 0];
  let i : Int = 0;
  while i < length(c) do (
	if i < length(a) then set c[i] = a[i] else set c[i] = b;
	set i = i + 1
  );
  set value = true ;
  value

let main(_) : Unit -> Unit =
  let s : Int[] = new Int[4 | 1];
  print(s)
