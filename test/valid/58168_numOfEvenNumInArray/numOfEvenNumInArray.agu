-- Author: 58168, Henrique Costa Vale

let f(a): Int[] -> Int =
  let value : Int = 0 ;
  let i : Int = 0 ;
  while i < length(a) do (
	if a[i] % 2 == 0 then set value = value + 1;
	set i = i + 1
  );
  value

let main(_) : Unit -> Unit =
  print(f(new Int[4 | 2]))
